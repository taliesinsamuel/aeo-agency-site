# Archived — early React MVP

Moved out of the repo root on purpose.

This is **not** the live AEO site. The default project is the static Attio build at the repo root (`127.0.0.1_8081/dl/` + `./serve.sh`).
