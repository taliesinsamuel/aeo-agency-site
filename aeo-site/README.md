# Deprecated — early React MVP

This folder is **not** the live AEO site.

It was an early incomplete React experiment. The real, up-to-date website is the static Attio-based build documented in the **root `README.md`** (`127.0.0.1_8081/dl/` + `build_hero.py`).

Do not continue work here unless you are intentionally rewriting the site in React from scratch.
