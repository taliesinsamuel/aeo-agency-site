{
  "name": "answered-labs-site",
  "private": true,
  "version": "1.0.0",
  "description": "Answered Labs production marketing site (static + Vercel API)",
  "scripts": {
    "build": "cd 127.0.0.1_8081/dl && python3 build_hero.py",
    "serve": "./serve.sh",
    "test:api": "node scripts/test-free-audit.js",
    "dev": "node scripts/local-site-server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
